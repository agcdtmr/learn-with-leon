//Create a function that takes in an array of numbers. Multiply each number together and alert the product.
// function someName(arr) {
//   let product = 1;
//   arr.forEach((num) => (product *= num));
//   alert(product);
// }
// someName();

function someFuncName(arr) {
  let product = 1;
  arr.forEach((num) => (product *= num));
  alert(product);
}

someFuncName(10, 20, 30);
