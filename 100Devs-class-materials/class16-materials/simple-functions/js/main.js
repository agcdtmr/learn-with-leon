//---Easy
//create a function that subtracts two numbers and alerts the difference
function subTwoNums(num1, num1) {
  alert(num1 - num2);
}

// subTwoNums(5, 10);
// subTwoNums(100, 50);
//create a function that divides three numbers and console logs the quotient
function divideThreeNums(n1, n2, n3) {
  console.log(n1 / n2 / n3);
}

divideThreeNums(5, 10, 15);
//create a function that multiplys three numbers and returns the product
function multiplyThreeNums(nu1, nu2, nu3) {
  // return will poof into the air and wont display in either alert or console.log
  return nu1 * nu2 * nu3;
}

// multiplyThreeNums(5, 10, 15);

// wrap your call & argument in a variable then it will

let product = multiplyThreeNums(5, 10, 15);

alert(product);

//---Medium
//create a function that takes in three numbers. Add the first two numbers and return the remainder of dividing the sum of the first two numbers by the third number

//---Hard
//create a function that takes in 4 numbers. Multiply the first two numbers. If the product is greater than 100 add the sum of the last two numbers and console log the value. If the product is less that 100, subtract the difference of the last two numbers and console log the value. If the product is 100, multiply the first three numbers together and alert the remainder of dividing the fourth number
